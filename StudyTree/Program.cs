using TreeStudy;

internal class Program
{
    private static void Main(string[] args)
    {
        var tree = new MyTree();
        tree.InsertInTree(1);
        tree.InsertInTree(3);
        tree.InsertInTree(2);
        tree.InsertInTree(5);
    }
}